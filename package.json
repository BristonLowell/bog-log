{
  "name": "bug-log",
  "version": "1.0.0",
  "scripts": {
    "setup": "npm run setup:client && npm run setup:server",
    "setup:client": "cd bug-log.client && npm i",
    "setup:server": "cd bug-log.server && npm i"
  }
}
